# GCAS Rust

A generic content addressable store written in rust

## How to run

```cargo build```

Type in this command into the terminal to make cargo build the object with all its dependencies

```cargo test```

To run the testing suite and watch it in action
